---
title: "Authentication"
description: "API key authentication and security best practices"
---

## API Keys

Authenticate all API requests with your API key in the `Authorization` header:

```bash
curl https://api.recurso.dev/v1/customers \
  -H "Authorization: Bearer rsk_live_xxx"
```

## Key Types

| Type | Prefix | Environment |
|------|--------|-------------|
| Live | `rsk_live_` | Production |
| Test | `rsk_test_` | Development |

<Warning>
**Never expose API keys in client-side code.** Keys should only be used on your backend.
</Warning>

## Generate Keys

1. Go to **Settings → API Keys** in your dashboard
2. Click **Create API Key**
3. Choose **Live** or **Test** mode
4. Copy and store securely

## Usage

Pass your API key in the `Authorization` header as a Bearer token:

```bash
curl https://api.recurso.dev/v1/customers \
  -H "Authorization: Bearer $RECURSO_API_KEY"
```

## Scoped Keys (Coming Soon)

Create keys with limited permissions via the dashboard or API.

## Security Best Practices

<CardGroup cols={2}>
  <Card title="Environment Variables" icon="leaf">
    Store keys in environment variables, not code
  </Card>
  <Card title="Rotate Regularly" icon="arrows-rotate">
    Rotate keys periodically and after breaches
  </Card>
  <Card title="Use Test Keys" icon="flask">
    Always use test keys for development
  </Card>
  <Card title="Restrict Permissions" icon="lock">
    Use scoped keys with minimal permissions
  </Card>
</CardGroup>

## Revoke a Key

If a key is compromised:

1. Go to **Settings → API Keys**
2. Click **Revoke** on the compromised key
3. Generate a new key
4. Update your applications

<Info>
Revoking a key is immediate and permanent.
</Info>
