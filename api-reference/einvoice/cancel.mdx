---
title: "Cancel E-Invoice"
openapi: "POST /v1/invoices/{id}/einvoice/cancel"
description: "Cancel a previously generated e-invoice on the IRP portal."
---

## Path Parameters

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `id` | string | Yes | The unique identifier of the invoice whose e-invoice should be cancelled (e.g., `inv_Rp3mK7nBwQ`). |

## Body Parameters

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `cancel_code` | integer | Yes | The cancellation reason code. Must be between 1 and 4. See codes below. |
| `reason` | string | Yes | A human-readable explanation for the cancellation. Minimum 10 characters. |

### Cancel Codes

| Code | Meaning |
|------|---------|
| `1` | Duplicate entry |
| `2` | Data entry mistake |
| `3` | Order cancelled |
| `4` | Others |

## Example Request

```bash
curl -X POST https://billing.example.com/v1/invoices/inv_Rp3mK7nBwQ/einvoice/cancel \
  -H "Authorization: Bearer rsk_live_7f3a9b2c1d4e8f0a" \
  -H "Content-Type: application/json" \
  -d '{
    "cancel_code": 2,
    "reason": "Incorrect line item amounts were included on the original invoice"
  }'
```

## Response

```json
{
  "message": "E-invoice cancelled successfully",
  "data": {
    "id": "einv_Ys5tH2xLmN",
    "invoice_id": "inv_Rp3mK7nBwQ",
    "irn": "a1b2c3d4e5f6a7b8c9d0e1f2a3b4c5d6e7f8a9b0c1d2e3f4a5b6c7d8e9f0a1b2",
    "status": "cancelled",
    "cancel_code": 2,
    "cancel_reason": "Incorrect line item amounts were included on the original invoice",
    "cancelled_at": "2026-06-23T16:10:45Z"
  }
}
```

## Notes

- E-invoices can only be cancelled within 24 hours of generation, as per IRP regulations.
- A cancelled e-invoice cannot be reinstated. You must generate a new invoice and submit it for e-invoicing.
- The `reason` field is transmitted to the IRP and becomes part of the permanent compliance record.

<Warning>
Cancellation is permanent and governed by IRP rules. Attempting to cancel an e-invoice older than 24 hours will return a `422 Unprocessable Entity` error.
</Warning>
